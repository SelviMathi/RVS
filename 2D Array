//DISPLAY 2D

#include <stdio.h>
int main()
{
 int row,col;
 scanf("%d %d",&row,&col);
int arr[row][col];
for(int i=0;i<row;i++)
{
    for(int j=0;j<col;j++)
    scanf("%d",&arr[i][j]);
}
for(int i=0;i<row;i++,printf("\n"))
{
    for(int j=0;j<col;j++)
    printf("%d ",arr[i][j]);
}
 
    return 0;
}

//MIN IN ROW
#include <stdio.h>
#include<limits.h>
int main()
{
 int row,col;
 scanf("%d %d",&row,&col);
int arr[row][col];
for(int i=0;i<row;i++)
{
    for(int j=0;j<col;j++)
    scanf("%d",&arr[i][j]);
}
 for(int i=0;i<col;i++)
 {
     int min=INT_MAX;
     for(int j=0;j<row;j++)
     if(arr[i][j]<min)
     min=arr[i][j];
    printf("%d ", min);
}
    return 0;
}



